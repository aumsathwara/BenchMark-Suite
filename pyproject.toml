[project]
name = "trial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp-all",
]

[tool.uv.sources]
mcp-all = { git = "https://github.com/aumsathwara/MCP.git", rev = "multiple-mcps" }
